login_attempts = [
    {"user": "alice", "success": True},
    {"user": "bob", "success": False},
    {"user": "bob", "success": False},
    {"user": "bob", "success": False},
    {"user": "alice", "success": True},
    {"user": "charlie", "success": False},
]

def find_suspicious_users(login_attempts, threshold):
    if suspicious_users is None:
        suspicious_users = []
    


                                
def track_failed_attempts(login_attempts, failed_attempts=none):
    for attempt in login_attempts: 
        if not ["success"]:


def sus_users():


def warning():

def log_summary():


#Count failed attempts.
#Identify users with at least 3 failed attempts using find_suspicious_users.
#Print a warning for those users.
#Print a summary of the log.
